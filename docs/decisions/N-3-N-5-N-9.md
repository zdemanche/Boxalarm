# N-3 / N-5 / N-9 (from #10) + #119 residual

## N-3
Remove duplicate `GET /api/v1/personnel/losap/year-end`. Reporting owns year-end.

## N-5
- `dispatch.alert.received` → `alerting.dispatch.received`
- `cert.expiry.due` → `training.expiry.due`
- `ppe.expiry.due` → `inventory.expiry.due`
- Leave `scheduling.coverage_gap.detected`

## N-9
- `PUT /api/v1/apparatus/{unitId}/defects/{defectId}/status`
- `PUT /api/v1/inspections/{id}/violations/{code}`
- `PUT /api/v1/inventory/consumables/{itemId}`

## #119 residual (post-#121)
- Remove `FanOut --> VoiceQ` (voice is escalator-only)
- Escalator: single edge to VoiceQ; gate on no ack across push **AND** sms
- Escalator label: voice tier only (not Step Functions poll / OR sms)

Endpoint count after net +2: **83 / 89**.
