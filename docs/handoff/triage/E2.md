| # | key | verdict | evidence (file paths) | gaps (one line, concrete) |
|---|---|---|---|---|
| 203 | E2-S1-INFRA | DONE | `infrastructure/components/personnel/members.ts`, `infrastructure/components/messaging/outbox-publisher.ts`, `infrastructure/index.ts:196` | none found — single consolidated outbox publisher resolves the "triple-publish" trap; IAM scoped to platform table only |
| 204 | E2-S2-INFRA | PARTIAL | `infrastructure/components/personnel/quals.ts`, `backend/src/services/personnel-service/events/certExpiredReactor.ts`, `backend/src/services/alerting-service/eligibility/eligibilityChangedConsumer.ts` | GET/PUT quals routes deployed; but no infra wiring anywhere for platform-table stream → certExpiredReactor, the `personnel.eligibility.changed` bus rule, the eligibility-snapshot queue+DLQ+alarm, or the alerting-service consumer — backend handlers exist but are undeployed |
| 141 | E2-S2-UI | DONE | `ui/apps/web/src/features/personnel/MemberDetailPage.tsx` (QualsSection), `ui/apps/mobile/src/screens/me/MeHomeScreen.tsx` | none found |
| 205 | E2-S3-INFRA | DONE | `infrastructure/components/personnel/attendance.ts`, `infrastructure/index.ts:219` | none found — record/record-on-behalf/query/query-on-behalf routes all deployed with IAM scoped to table + GSI1 |
| 263 | E2-S3-UI | DONE | `ui/apps/mobile/src/screens/me/AttendanceScreen.tsx`, `ui/apps/mobile/src/features/attendance/apiAttendanceRepository.ts`, `ui/apps/web/src/features/personnel/MemberDetailPage.tsx` (AttendanceSection) | idempotency relies on server-side `occurredAt`+409-suppression rather than an explicit client idempotency key, but AC1 behavior (queued record posts once) is met |
| 206 | E2-S4-INFRA | PARTIAL | `infrastructure/components/personnel/losap.ts`, `backend/src/services/personnel-service/attendance/handler.ts:161-213` | member-total/update-rules/year-end routes deployed, but the spec'd `personnel.attendance.recorded`→accrual-queue→consumer chain (with EVENT_DEDUP, DLQ, alarm) doesn't exist anywhere; LOSAP points are instead computed synchronously inline in the attendance handler with no queue, dedup entity, or DLQ |
| 142 | E2-S4-UI | DONE | `ui/apps/web/src/features/losap/LosapSettingsPage.tsx`, `ui/apps/web/src/features/personnel/MemberDetailPage.tsx` (LosapSection), `ui/apps/mobile/src/screens/me/MeHomeScreen.tsx` | none found |
| 209 | E2-S7-INFRA | DONE | `infrastructure/components/personnel/shifts.ts`, `backend/src/services/personnel-service/shifts/handler.ts:272-280` | AC3 (member POST→403) is enforced via a `cognito:groups` check, not Cedar `IsAuthorizedWithToken` as architecture specifies (TODO at handler.ts:278/860) — functional behavior is correct, mechanism deviates |
| 145 | E2-S7-UI | DONE | `ui/apps/web/src/features/schedule/SchedulePage.tsx`, `ui/apps/web/src/routing/routeTable.ts:152-157` | none found — `/schedule` restricted to OFFICER/ADMIN via RequireRole, list+create wired to real API |
| 210 | E2-S8-INFRA | PARTIAL | `infrastructure/components/personnel/shifts.ts`, `backend/src/services/personnel-service/shifts/claimShiftPosition.ts`, `backend/src/services/personnel-service/shifts/claimShiftPosition.test.ts:48` | route + atomic conditional-write claim logic deployed; AC1's Tier-1 concurrency benchmark against a real deployed qa stack is explicitly not implemented — test file comment says it's "a tracked residual owned by the boxalarm-infrastructure/e2e suite" |
| 146 | E2-S8-UI | PARTIAL | `ui/apps/mobile/src/screens/schedule/ShiftDetailScreen.tsx`, `ui/apps/mobile/src/features/schedule/apiScheduleRepository.ts:9-46` | claim/offline-pending/reconnect-resubmit/ALREADY_TAKEN-announce all implemented; but `getShifts()` always returns `positions: []` for real-backend shifts ("no member-facing positions read endpoint yet" per code comment), so on real data there is nothing to claim against |
| 211 | E2-S9-INFRA | PARTIAL | `infrastructure/components/personnel/shifts.ts:25-78`, `backend/src/services/personnel-service/shifts/handler.ts:854-910` | coverage route deployed and functionally correct, but AC3 ("role holds no write action") fails — coverage shares one Lambda/IAM role with claim/release/swap (PutItem/UpdateItem granted) since shifts.ts intentionally uses a single combined router Lambda |
| 147 | E2-S9-UI | DONE | `ui/apps/web/src/features/schedule/SchedulePage.tsx` (CoverageSection) | none found — covered/short/qual-gapped rendered with text+StatusChip (not colour alone), no reassign/callback controls present |
| 212 | E2-S10-INFRA | DONE | `infrastructure/components/personnel/shifts.ts` (proxy route), `backend/src/services/personnel-service/shifts/handler.ts:738-825`, `shiftSwap.ts`, `releaseShiftPosition.ts` | none found — release/swap/officer-approve/target-accept all implemented with correct `requiresOfficerApproval` branching |
| 148 | E2-S10-UI | PARTIAL | `ui/apps/mobile/src/screens/schedule/ShiftDetailScreen.tsx`, `ui/apps/web/src/features/schedule/SchedulePage.tsx` (SwapApprovalForm) | mobile: give-back + propose-swap exist but no UI/repository method for the target member to see/accept an incoming swap (AC3 not built); web: only a manual shiftId/swapId approve text form, not a "pending swap requests list" (scope's own wording); both also hit #146's `positions: []` gap |
| 213 | E2-S11-INFRA | PARTIAL | `backend/src/services/personnel-service/shifts/completionHandler.ts` | backend completion handler exists, but infra has zero EventBridge Scheduler wiring anywhere (`grep -ri scheduler infrastructure/` finds none for this job) — no schedule, no Lambda deployment, no DLQ, no alarm |
| 207 | E2-S5-INFRA | PARTIAL | `infrastructure/components/personnel/availability.ts`, `infrastructure/components/alerting/staleness.ts`, `backend/src/services/alerting-service/eligibility/consumer.ts` | mark-off create route + two one-time EventBridge Scheduler schedules + expiry Lambda deployed (AC1), and the shared 5-min staleness alarm covers AC4; but the `personnel.availability.changed` bus rule → `availability-snapshot-queue`+DLQ → alerting-service consumer (AC2/AC3, the actual alerting-suppression path) is not wired anywhere — `addQueueConsumer` is called only once in the whole repo, for session revocation, not this |
| 208 | E2-S6-INFRA | PARTIAL | `infrastructure/components/personnel/members.ts` (`updateProfileLambda`), `backend/src/services/alerting-service/eligibility/memberUpdatedHandler.ts` | `PUT /members/{memberId}` route deployed (AC1); but the `personnel.member.updated` → `member-snapshot-queue`+DLQ → alerting-service snapshot handler (AC2/AC3, updating `contactChannels`) is not wired — the only deployed consumer of that event is session-revocation's separate queue (`infrastructure/components/identity/session-revocation.ts`), which the issue itself calls out as a different queue owned by E8-S8-INFRA |
| 143 | E2-S5-UI | PARTIAL | `ui/apps/mobile/src/screens/schedule/AvailabilityScreen.tsx` | wired to real `markUnavailable` API, has offline-pending state, endAt≤startAt validation, and announced success — but scope says "replace the fixed defaultStart()/defaultEnd() with real start/end date-time pickers" and the screen instead uses plain `TextInput` text fields for date/time, acknowledged in-code as a `ponytail:` shortcut ("no date-picker dependency is installed yet") |
| 144 | E2-S6-UI | PARTIAL | `ui/apps/mobile/src/screens/me/ProfileEditScreen.tsx`, `ui/apps/mobile/src/screens/me/MeHomeScreen.tsx`, `ui/apps/mobile/src/features/me/apiMeRepository.ts` | edit screen is real, labeled, 403-handled, and touch targets meet the design token baseline; but AC1 ("Me shows the new values") doesn't hold — `MeHomeScreen` fetches the profile only in a mount-only `useEffect` (no `useFocusEffect`/navigation listener), so returning from a successful save still shows the stale cached profile until the app remounts |
