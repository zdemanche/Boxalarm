| # | key | verdict | evidence (file paths) | gaps (one line, concrete) |
|---|---|---|---|---|
| 260 | E8-S9-INFRA | PARTIAL | infrastructure/components/platform/retention.ts; backend/src/services/platform-service/retention/{disposal.ts,disposalHandler.ts} | Daily EventBridge Scheduler invokes disposalHandler.ts directly, but the handler requires an API-Gateway-shaped bearer token + routeKey (`retention.ts:196-211` own comment confirms) — the scheduled invocation 401/404s and disposes nothing; only the manual admin route works. Also `runDisposal` has no built-in scan for past-retention records — it only acts on caller-supplied `candidates` locators, so nothing auto-discovers what's due. |
| 177 | E8-S9-UI | DONE | ui/apps/web/src/features/platform/RetentionSection.tsx, SettingsPage.tsx; ui/apps/web/src/routing/routeTable.ts (`/settings` roles: ['ADMIN']) | none — retention editor, life-safety class list, and disposal button (plain `window.confirm`, no re-auth) all present; route is ADMIN-only so the control is unreachable for non-admins. |
| 259 | E8-S8-INFRA | DONE | infrastructure/index.ts:82-99 (1h/1h/3650d, revocation, rotation grace); infrastructure/components/identity/session-revocation.ts; backend/src/services/platform-service/session-revocation/{memberStatusRevocationHandler.ts,deviceLossHandler.ts,cognitoRevocationClient.ts} | none found against stated ACs. |
| 176 | E8-S8-UI | PARTIAL | ui/apps/web/src/features/personnel/MemberDetailPage.tsx (canRevokeSessions, revoke action); ui/apps/web/tests/e2e/auth.spec.ts; ui/apps/mobile/src/auth/AuthContext.test.tsx | Scope explicitly calls for a "Native instrumented test (XCTest/Espresso)" proving the rotated refresh token persists in Keychain/Keystore across app restart — only JS-level Jest tests mocking Keychain exist, no native instrumented test found. |
| 258 | E8-S6-INFRA | DONE | infrastructure/components/platform/export.ts; backend/src/services/platform-service/export/{handler.ts,worker.ts} | none — read-only worker role grants no writes, unconditional ExportInvoked alarm wired to chief SNS topic, 7-day bucket lifecycle present. |
| 257 | E8-S5-INFRA | PARTIAL | infrastructure/components/data/platform-table.ts (`auditMutationDenyStatement`); infrastructure/components/data/audit-trail.ts; backend/src/services/platform-service/audit/{handler.ts,auditEntry.ts,queryAuditTrail.ts} | AC4 ("every mutating LOB service role gets an explicit Deny") is applied to only 3 of ~15+ platform-table-mutating Lambda roles (retention, config, personnel/members) — training/events, training/certifications, personnel/{attendance,availability,losap,shifts,quals}, incident/{incident,schema-refresh} write the platform table with no `auditMutationDenyStatement`, so those roles could still tamper with/delete `DEPT#*#AUDIT#*` items. |
| 174 | E8-S5-UI | DONE | ui/apps/web/src/features/platform/AuditLogPage.tsx; ui/apps/web/src/routing/routeTable.ts (`/audit-log` roles: ['ADMIN','CHIEF']) | none — entity lookup, diff rendering, cursor "Load more", and role-restricted route all present. |
| 256 | E8-S4-INFRA | PARTIAL | infrastructure/components/platform/config.ts (self-documented); infrastructure/components/messaging/outbox-publisher.ts | ElastiCache Serverless Valkey + VPC + gateway endpoints are deliberately NOT provisioned — component's own comment says the backend has no Valkey client yet (`config/cache.ts` always uses in-memory fallback) so wiring a cache nothing reads would be idle cost; ships without AC2's Valkey-unavailable fallback path entirely (there's nothing to fail over from). Config route + outbox publish of `platform.config.alert_rules.updated` are done. |
| 254 | E8-S2-INFRA | DONE | infrastructure/components/identity/{user-pool.ts,recovery-monitor.ts}; backend/src/services/platform-service/... credential-recovery-monitor/handler.ts | none — accountRecoverySetting, SMS role, CloudTrail→EventBridge→monitor Lambda, and Recovery* alarms all wired. |
| 171 | E8-S2-UI | PARTIAL | ui/apps/web/src/pages/SignInPage.tsx (+ .test.tsx); ui/apps/mobile/src/screens/SignInScreen.tsx | Scope explicitly calls for a "Playwright Tier 1 flow ... recovery E2E" — no recovery E2E spec exists under ui/apps/web/tests/e2e (only component-level SignInPage.test.tsx and an unrelated sign-in axe test in auth.spec.ts). |
| 253 | E7-S9-INFRA | NOT_STARTED | (searched infrastructure/components/reporting/reporting.ts, backend/src/services/reporting-service/) | No reporting export route, worker Lambda, or DLQ/alarm exists for reporting-service; only the unrelated platform-wide export (E8-S6, issue 258) is built. |
| 252 | E7-S7-INFRA | DONE | infrastructure/components/reporting/reporting.ts (membershipTrendsLambda, GET /api/v1/reporting/membership-trends, Query-only IAM); backend/src/services/reporting-service/membershipTrends/handler.ts | none found. |
| 267 | E7-S6-INFRA | NOT_STARTED | (no matching route/handler under infrastructure/components/reporting or backend/src/services/reporting-service) | `GET /api/v1/reporting/response-times` route, Lambda, and incident-GSI1 IAM do not exist anywhere on main. |
| 251 | E7-S5-INFRA | PARTIAL | infrastructure/components/reporting/reporting.ts:30-41 (self-documented); backend/src/services/reporting-service/grants/assembleReport.ts | Component's own comment: the incident-table + CMK grant for the incident-volume figure is "not wired" — `assembleReport.ts` hardcodes `incidentVolume: { available: false, reason: 'E6-S1' }`, so AC1's "read all four source domains" is only 3 of 4. |
| 250 | E7-S4-INFRA | NOT_STARTED | (no iso route/handler under infrastructure/components/reporting or backend/src/services/reporting-service) | `GET /api/v1/reporting/iso` route and Lambda do not exist. |
| 249 | E7-S3-INFRA | DONE | infrastructure/components/reporting/reporting.ts (losapYearEndLambda, GET /api/v1/reporting/losap/year-end, Query-only); backend/src/services/reporting-service/losap/{handler.ts,repository.ts} | none found. |
| 248 | E7-S2-INFRA | NOT_STARTED | (searched for "reporting-projection", "ReportingProjection", "EVENT_DEDUP" repo-wide — no matches outside unrelated alerting-service files) | No reporting-projection-queue, rollup Lambda, or bus rules for the named event types exist anywhere. |
| 247 | E7-S1-INFRA | NOT_STARTED | (no dashboard route/handler under infrastructure/components/reporting or backend/src/services/reporting-service) | `GET /api/v1/reporting/dashboard` route and Lambda do not exist; depends on the also-missing E7-S2 rollups. |
| 101 | E8-S1 | DONE | backend/src/services/platform-service/authorizer/{tokenVerifier.ts,handler.ts,tokenVerifier.test.ts}; infrastructure/components/identity/user-pool.ts (`mfaConfiguration: "OFF"`); ui/apps/web & ui/apps/mobile package.json (oidc-client-ts, react-native-app-auth) | none — authorizer validates signature/client_id(not aud)/issuer/expiry with extensive unit tests (expired, bad sig, wrong client, wrong issuer); MFA explicitly OFF. |
| 100 | E7-S9 | NOT_STARTED | (same search as 253) | Parent depends on E7-S1/S3-S7 endpoints, most of which (dashboard, iso, response-times) don't exist; no export code for any report exists either. |
| 117 | E7-S8 | NOT_STARTED | ui/apps/web/src/App.tsx:186 (`<Route path="reporting" element={placeholder('Reporting')} />`) | The `/reporting` route is a bare placeholder page — no dashboard panels, no report screens, no date-range/filter controls, no export path exist. |
| 99 | E7-S6 | NOT_STARTED | (no response-time analytics compute code under backend/src/services/reporting-service) | Turnout/travel/total computation and percentile aggregation do not exist in reporting-service. |
| 97 | E7-S4 | NOT_STARTED | (no iso aggregation code under backend/src/services/reporting-service) | Cross-domain ISO report aggregation (training/apparatus/hydrant/response-time) does not exist. |
| 94 | E7-S1 | NOT_STARTED | (no dashboard handler under backend/src/services/reporting-service); ui/apps/web/src/App.tsx:186 (placeholder) | Neither the rollup-read dashboard endpoint nor the UI panels exist. |
| 115 | Bootstrap boxalarm-ui | PARTIAL | ui/apps (mobile + web scaffolded, 24 web routes); ui/packages/ (only design-tokens); ui/apps/mobile/src/sync/{outbox.ts,syncManager.ts,db.ts} | No shared `@boxalarm/core` or `@boxalarm/i18n` packages exist (only `@boxalarm/design-tokens`) — offline sync engine is built app-local under ui/apps/mobile/src/sync/ instead of as the shared package the checklist calls for; no i18n infrastructure found at all; no custom native modules for iOS Critical Alerts (Notification Service Extension) or Android full-screen intent/high-priority channel — only default RN AppDelegate.swift/MainActivity.kt scaffolding (consistent with entitlement #4 still being blocked). |
| 178 | Bootstrap boxalarm-infrastructure | DONE | infrastructure/Pulumi.{dev,qa,staging,prod}.yaml (us-east-1 each); infrastructure/index.ts (platformTable/incidentTable/alertingTable, Cognito, Verified Permissions); infrastructure/components/alerting/messaging-alerting.ts (SNS FIFO); infrastructure/components/data/{incident-table.ts,alerting-table.ts} (CMKs); infrastructure/components/neris/neris-config.ts | none found against the checklist — all bullet items present. |
