| # | key | verdict | evidence (file paths) | gaps (one line, concrete) |
|---|---|---|---|---|
| 40 | E1-S15 (parent) | PARTIAL | backend/src/services/alerting-service/audit/deliveryBaseline.ts, deliveryBaselineHandler.ts; ui/apps/web/src/App.tsx:186 | Delivery-rate/missed-page/time-to-first-ack metrics computed (AC2), but no decision-write/threshold-met route (AC3) and no retained-paging admin notice (AC4); web `/reporting` is still a placeholder route. |
| 37 | E1-S12 (parent) | PARTIAL | backend/src/services/alerting-service/diagnostics/{handler,selfHandler,queryDiagnostics}.ts | Backend timeline/roster-eligibility diagnosis logic exists and is routed (see #229/#160 infra), but no UI consumes it — see #160. |
| 35 | E1-S10 (parent) | DONE | backend/src/services/alerting-service/canary/{handler,statusHandler,canaryRunRepository}.ts | Reuses self-test pipeline (`createManualDispatch`/`SELF_TEST_CHANNELS`), records result/latencyMs/channelResults, emits EMF directly (bypasses SQS), status route exists. All ACs backend-testable and met. |
| 116 | E1-S7 | PARTIAL | ui/apps/mobile/src/features/alerts/{nativePushBridge,pushNotificationDisplay}.ts; ui/apps/mobile/ios/Boxalarm/AppDelegate.swift; ui/apps/mobile/android | JS-level critical-alert permission request, `AndroidImportance.HIGH` channel + `fullScreenAction` via notifee, and graceful degradation (`CRITICAL_ALERTS_ENTITLEMENT_GRANTED` check) all exist; but the scoped hand-written iOS Notification Service/Content Extension and Android `FirebaseMessagingService` are absent (no such files anywhere in ios/ or android/), so AC3 ("delivery doesn't depend on RN JS thread running") is unproven. |
| 235 | E1-S18-INFRA | PARTIAL | infrastructure/components/alerting/riding-board.ts | Board get/assign routes deployed, but against the platform table via apparatus-service (documented deliberate deviation from ticket's alerting-table-only scope); the apparatus-status→alerting copy queue and the board-assignment→platform-bus bridge event are explicitly deferred/not built (parent AC4, AC6). |
| 266 | E1-S18-UI | DONE | ui/apps/mobile/src/screens/alerts/RidingBoardScreen.tsx; ui/apps/web/src/features/alerts/AlertsRosterPage.tsx | Mobile screen uses real API (`useAlertsRepository`); web board has seat assignment, unqualified-assignment flagging, OOS exclusion, and conflict-error handling wired. |
| 162 | E1-S17-UI | DONE | ui/apps/mobile/src/features/alerts/PrePlanPanel.tsx | All four blocks (summary, hazards, utility shutoffs, hydrants) render after core content; renders nothing when `prePlan === undefined` (no empty shell/spinner). |
| 234 | E1-S15-INFRA | PARTIAL | infrastructure/components/alerting/routes-ops.ts (`deliveryBaseline` route) | Only a read-only baseline-metrics GET route exists, and it lives in alerting-service rather than the scoped reporting-service; no decision-write (accept/defer) persistence route exists anywhere in reporting-service or alerting-service. |
| 161 | E1-S15-UI | NOT_STARTED | ui/apps/web/src/App.tsx:186 (`/reporting` placeholder); ui/apps/web/src/features/platform/SettingsPage.tsx | No cutover report, no decision form, and no retained-paging notice on `/settings` (grep for tone-out/retained/decider found nothing). |
| 233 | E1-S14-INFRA | PARTIAL | infrastructure/components/alerting/push-tokens.ts; infrastructure/components/personnel/members.ts:196; backend/src/services/personnel-service/members/updateStatus.ts | Register/revoke routes, `personnel.member.updated`→snapshot consumer, and push receipt webhook all deployed. But the deployed `PUT .../status` route is bound to `members-update-status` (E2-S1's handler), which has no push-token revocation logic — `pushTokens/statusChange.ts` (the E1-S14 handler that revokes tokens) is never wired to any route, so parent AC5 ("member set inactive → tokens revoked") is unmet — exactly the handler conflict the ticket flagged as blocking. |
| 265 | E1-S14-UI | DONE | ui/apps/mobile/src/features/alerts/{nativePushBridge,pushTokens}.ts; ui/apps/mobile/src/auth/AuthContext.tsx:258-266 | Token acquisition/rotation via `@react-native-firebase/messaging` + `@notifee/react-native` (library-based rather than hand-written native code, but functionally equivalent); POST/DELETE wired; sign-out sends DELETE before clearing credentials. |
| 232 | E1-S13-INFRA | PARTIAL | infrastructure/components/alerting/iam-boundary.ts + infrastructure/test/alerting/iam-boundary.test.ts; backend/src/services/alerting-service/eligibility/staleness/checkHandler.ts; infrastructure/components/alerting/staleness.ts | Permissions boundary + Pulumi test done. Staleness schedule/alarm deployed, but `checkHandler.ts` still measures `now - snapshotUpdatedAt > 15min` — the exact false-positive-prone logic the ticket's own "Current state" flagged as needing a fix before the alarm is meaningful. No notification-service infra exists at all in `infrastructure/components` (grep found zero matches), so the required notification-service isolation Pulumi test cannot exist either. |
| 160 | E1-S12-UI | NOT_STARTED | ui/apps/web/src/App.tsx:162 (`/alerts/diagnostics` placeholder); ui/apps/mobile/src/screens/me/DiagnosticsScreen.tsx:31 ("not yet connected") | Backend diagnostics API (#37/#229) exists but neither web nor mobile UI calls it. |
| 231 | E1-S11-INFRA | DONE | infrastructure/components/alerting/{channel-workers,alarms}.ts | Per-channel queue/DLQ/concurrency/secret isolation; `alerting-page` SNS topic; per-channel DLQ-not-empty and delivery-failure-rate alarms page it; fault-injection SSM params scoped to `NON_PROD_ENVS` only. |
| 230 | E1-S10-INFRA | DONE | infrastructure/components/alerting/canary.ts | Schedule, failure/latency alarms (`TreatMissingData: breaching`), and `Boxalarm/AlertingCanary` namespace match what `canary/handler.ts` actually emits — the namespace mismatch the ticket flagged is reconciled. |
| 159 | E1-S10-UI | NOT_STARTED | ui/apps/web/src/App.tsx:162 | Depends on `/alerts/diagnostics`, which is a placeholder — no canary health panel exists. |
| 229 | E1-S9-INFRA | DONE | infrastructure/components/alerting/routes-ops.ts (`audit` route) | `GET /api/v1/alerting/audit` with `Query`-only IAM scoped to the table + GSI1/GSI2 index ARNs explicitly, no write actions. |
| 228 | E1-S8-INFRA | DONE | infrastructure/components/alerting/{routes-ops,channel-workers}.ts | Self-test POST/GET routes; per-channel sandbox secrets (`sandboxSecrets`) separate from production secrets, readable only by that channel's worker. |
| 158 | E1-S8-UI | DONE | ui/apps/mobile/src/screens/me/SelfTestScreen.tsx | Consolidated single entry point (comment confirms Alerts tab redirect removed), per-channel pass/fail with timestamp and specific failure reason, never touches alert history/roster. |
| 227 | E1-S6-INFRA | DONE | infrastructure/components/alerting/routes-core.ts (`detail` route) | `GET /api/v1/alerting/dispatches/{dispatchId}` with read-only IAM (`GetItem`/`Query`) only. |
| 157 | E1-S6-UI | DONE | ui/apps/mobile/src/screens/alerts/AlertDetailScreen.tsx | Incident type (header, announced first), address, cross streets, map link (`Linking.openURL`), narrative all render from the real API; no loading gate on enrichment (PrePlanPanel renders independently). |
| 226 | E1-S5-INFRA | DONE | infrastructure/components/alerting/routes-core.ts (`responses`, `roster` routes) | Response POST and roster GET routes, alerting-table-only IAM, reserved concurrency separate from fan-out/workers. |
| 156 | E1-S5-UI | DONE | ui/apps/mobile/src/screens/alerts/AlertDetailScreen.tsx | Responding/Direct-to-scene/Not-responding with ETA entry, optimistic confirm via real API (`repository.submitResponse`); web roster confirmed live via `AlertsRosterPage.tsx`. |
| 225 | E1-S4-INFRA | DONE | infrastructure/components/alerting/routes-ops.ts (webhooks loop, `receiptsGet`) | SMS/voice/push webhook routes each with their own Pulumi-secret and no Cognito authorizer; receipts GET route with admin authz. Deviates from the ticket's scoped SNS-publish→receipts-queue.fifo→processor pattern — webhooks write the delivery receipt directly instead — but this is a structural simplification that still satisfies channel isolation (no queue path exists for receipts to leak into push/sms/voice queues) and all stated ACs. |
| 155 | E1-S4-UI | DONE | ui/apps/web/src/features/alerts/AlertsRosterPage.tsx | Per-member/per-channel receipts table with a distinct `SENT_UNCONFIRMED` state (not colour-only), refetches on interval. |
| 224 | E1-S3-INFRA | DONE | infrastructure/components/alerting/{messaging-alerting,escalation,channel-workers}.ts | Voice FIFO queue+DLQ, `channel`-only filter; escalation schedule group with `scheduler:CreateSchedule`/`PassRole` scoped to that group only. |
| 223 | E1-S2-INFRA | DONE | infrastructure/components/alerting/{messaging-alerting,fan-out,channel-workers,iam-boundary}.ts | FIFO topic w/ content-based dedup off, per-channel queues filtered on `channel` only, fan-out/worker IAM scoped to alerting table + own secret only, isolation boundary denies platform/incident table access. |
| 114 | bug: no alerting consumer for personnel.eligibility.changed | PARTIAL | backend/src/services/alerting-service/eligibility/eligibilityChangedConsumer.ts; infrastructure/components/alerting/push-tokens.ts | The consumer Lambda (dedup, staleness-safe snapshot merge, quals add/remove) is fully written and unit-tested, but infra only wires `personnel.member.updated` → a queue/consumer (push-tokens.ts); no EventBridge rule/queue subscribes `personnel.eligibility.changed` to `eligibilityChangedConsumer.ts` anywhere in `infrastructure/components`, so the described defect still reproduces end-to-end in a deployed stack. |
| 24 | docs: department tone escalation + dedup-key defect | DONE | backend/src/services/alerting-service/fanout/idempotencyKey.ts; backend/src/services/alerting-service/escalation/{toneLadder,toneEvaluatorHandler,mutualAidPort}.ts | Dedup/idempotency key is `{dispatchId}#{toneSequence}#{memberId}#{channel}` (toneSequence present, fixing the silent tone-2/3 no-op defect); department-level tone-2 (T+180s)/tone-3 (T+360s)/mutual-aid-after-tone-3 fully implemented with configurable thresholds and officer-prompt mutual-aid adapter. |
| 12 | docs: verify final 4 architecture edits | DONE | infrastructure/components/alerting/messaging-alerting.ts:100-101; backend/src/services/alerting-service/fanout/{idempotencyKey.ts,fanOut.ts}; backend/src/services/alerting-service/fanout/handler.integration.test.ts:144-147 | All 5 checklist items verified in code: one publish per {member,channel} (fanOut.ts writes separate push/sms receipt items); `MessageDeduplicationId` keys on `channel` (+toneSequence), never `channelTier`; every queue subscription filters on `channel` only; receipt sk is `RECEIPT#{memberId}#{channel}#{toneSequence}` (channel present, toneSequence per the documented amendment); integration test asserts 2 distinct SNS publishes (push, sms) for one dispatch/one member. |
