| # | key | verdict | evidence (file paths) | gaps (one line, concrete) |
|---|---|---|---|---|
| 202 | E5-S8-INFRA | PARTIAL | `backend/src/services/alerting-service/dispatches/getDispatchHandler.ts` (embeds `getPrePlanCopy` from `prePlanCopyRepository.ts`), route `GET /api/v1/alerting/dispatches/{dispatchId}` deployed in `infrastructure/components/alerting/routes-core.ts` under the alerting-service IAM boundary; `backend/src/services/alerting-service/prePlan/getPrePlanPanelHandler.ts` also exists but is unused | No dedicated pre-plan Lambda/route/IAM-role/chaos-test as scoped — reconciled instead onto the existing dispatch-GET route; and since E5-S4-INFRA (#198) is undeployed, no PRE_PLAN_COPY item is ever written, so the query returns nothing in practice. |
| 139 | E5-S8-UI | PARTIAL | `ui/apps/web/src/features/alerts/PrePlanPanel.tsx`, `ui/apps/mobile/src/features/alerts/PrePlanPanel.tsx`, mounted in `AlertDetailScreen.tsx` and `AlertsRosterPage.tsx` | No Playwright spec exists asserting zero `/api/v1/inspections` network calls (AC4) — no alerts e2e spec found in `ui/apps/web/tests/e2e/` at all; component reads `prePlan` via `alerting/dispatches/{id}` GET enrichment, not a dedicated pre-plan route (functionally fine but diverges from ticket's named route). |
| 201 | E5-S7-INFRA | PARTIAL | `backend/src/services/inspections-service/fieldCapture/{handler,repository,payload}.ts` — idempotent conditional put (`ConditionExpression: attribute_not_exists(pk)`), signed-URL prefix `INSPECTION_RECORD/{id}/` | No Pulumi Lambda/route/IAM role for field-capture anywhere in `infrastructure/` — zero matches for fieldcapture/field-capture in infra. |
| 138 | E5-S7-UI | PARTIAL | `ui/apps/mobile/src/screens/checks/FieldCaptureScreen.tsx` (idempotency key, `AccessibilityInfo.announceForAccessibility`), `ui/apps/mobile/src/sync/SyncStatusBanner.tsx` | Screen imports `mockSyncRepository` (comment: "stands in for the outbox's drain-on-reconnect"), not the real `@boxalarm/core` outbox — no `@boxalarm/core` outbox package exists under `ui/packages` at all, so the E4-S3 dependency itself is unimplemented; writes aren't wired to `POST /api/v1/inspections/field-capture`. |
| 200 | E5-S6-INFRA | NOT_STARTED | `backend/src/services/inspections-service/map/{handler,dynamoClient,geohash,queryMapCells}.ts` (backend logic complete) | No Pulumi Lambda/route/IAM role for the map handler in `infrastructure/` — zero infra references to map/GSI3 for inspections. |
| 137 | E5-S6-UI | PARTIAL | `ui/apps/web/src/features/inspections/{MapPage,LeafletMap,mapProvider}.tsx`, route wired in `App.tsx`/`routeTable.ts`, pan/zoom controls, list view, OOS distinct by icon+label | Ticket scope explicitly required a `MapProvider` port with a **stub renderer and no vendor SDK** (OQ-20 unresolved); `MapPage.tsx` instead imports `LeafletMap.tsx` which pulls in `leaflet`/`react-leaflet` directly, bypassing the `MapProvider` interface defined in `mapProvider.ts` — a real vendor import contrary to AC1/AC2's explicit constraint. |
| 199 | E5-S5-INFRA | NOT_STARTED | `backend/src/services/inspections-service/{listInspections,recordInspection}/handler.ts` (backend logic complete, GSI2 casing consistent with table def) | No Pulumi Lambda/route/IAM role for inspection scheduling/conduct in `infrastructure/`. |
| 136 | E5-S5-UI | DONE | `ui/apps/web/src/features/inspections/InspectionsPage.tsx` — due list, schedule form, conduct form with violations, conductedDate/conductedBy/violations detail; route wired in `App.tsx` | none found (UI-side code matches all ACs; blocked only on backend infra deploy, which is out of this issue's scope) |
| 198 | E5-S4-INFRA | PARTIAL | `backend/src/services/alerting-service/prePlan/{prePlanCopyHandler,hydrantCopyHandler,prePlanCopyRepository}.ts` (SQS consumer handlers coded, isolation test `prePlan/isolationBoundary.test.ts` exists), `backend/src/services/inspections-service/hydrant/hydrantRepository.ts` uses `@boxalarm/outbox` `buildOutboxRecord` (entityType defect appears fixed) | No `preplan-copy-queue` SQS/DLQ, no EventBridge bus rule for `inspections.preplan.updated`/`inspections.hydrant.updated`, and no consumer Lambda deployment anywhere in `infrastructure/` — the coded consumers are never wired to the bus, so PRE_PLAN_COPY items never actually get written. |
| 197 | E5-S3-INFRA | NOT_STARTED | `backend/src/services/inspections-service/hydrant/{listHydrantsHandler,createHydrantHandler,updateHydrantHandler,hydrantRepository}.ts` (backend logic complete, transactional outbox write present) | No Pulumi Lambda/route/IAM role for hydrant routes in `infrastructure/`. |
| 135 | E5-S3-UI | DONE | `ui/apps/web/src/features/inspections/HydrantsPage.tsx` — create form, flow-test update, mark-OOS, OOS distinguished by icon+text label; route wired | none found (UI-side code matches all ACs; blocked only on backend infra deploy) |
| 196 | E5-S2-INFRA | PARTIAL | `backend/src/services/inspections-service/{getPrePlanHandler,putPrePlanHandler,prePlanRepository}.ts` (backend logic complete); generic outbox drain already deployed via `infrastructure/components/messaging/outbox-publisher.ts` wired in `infrastructure/index.ts` (satisfies the "single drain" requirement) | No `nichols-boxalarm-platform-assets` S3 bucket and no CloudFront distribution/OAC/signing-key-secret anywhere in `infrastructure/` (grep for platform-assets/CloudFront across `infrastructure/components` returns nothing relevant); no pre-plan Lambda/route deployed either. |
| 134 | E5-S2-UI | DONE | `ui/apps/web/src/features/inspections/OccupancyDetailPage.tsx` — pre-plan tab: diagram + multi-file attachment upload via signed URLs, utility shutoffs, hazards, read view; `ui/apps/web/src/features/inspections/inspections.test.tsx` | none found (UI-side code matches all ACs; blocked only on backend infra deploy) |
| 195 | E5-S1-INFRA | PARTIAL | `backend/src/services/inspections-service/occupancy/{handler,repository,authorization,config}.ts` (routes + AUDIT_LOG_ENTRY txn + 403 all coded) | No Pulumi wiring anywhere in `infrastructure/` (no occupancy Lambda/route/IAM role) — only the log-group name string in `infrastructure/components/observability/services.ts`; `occupancy/config.ts` still reads `OCCUPANCY_TABLE_NAME`, not `PLATFORM_TABLE_NAME`, unnormalized. |
| 133 | E5-S1-UI | DONE | `ui/apps/web/src/features/inspections/{OccupancyListPage,OccupancyDetailPage}.tsx` — list/detail (address, type, contacts, hazards), create/edit form, `canWrite` role gate (`ADMIN`/`CHIEF`) hides controls + `ApiForbiddenGate` for forced-write 403; route wired | none found (UI-side code matches all ACs; blocked only on backend infra deploy) |
