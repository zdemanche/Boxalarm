| # | key | verdict | evidence (file paths) | gaps (one line, concrete) |
|---|---|---|---|---|
| 246 | E6-S11-INFRA | DONE | `infrastructure/components/incident/schema-refresh.ts` | Bucket (Block Public Access, SSE-S3, versioning disabled, 7-day abort-incomplete-multipart, Intelligent-Tiering), daily EventBridge Scheduler, IAM scoped to `s3:PutObject` on `neris-schema/*` only (no DeleteObject), DynamoDB Put/Update+CMK, and an Errors alarm are all present. |
| 245 | E6-S10-INFRA | DONE | `infrastructure/components/incident/incident.ts` (search/get lambdas), `backend/src/services/incident-service/searchIncidents.ts`, `getIncident.ts` | `GET /api/v1/incidents` and `/{incidentId}` routes wired; search IAM grants `Query` on `GSI1` only (comment confirms no Scan); get IAM grants GetItem/Query + CMK decrypt + VP auth. |
| 170 | E6-S10-UI | NOT_STARTED | `ui/apps/web/src/App.tsx` (`incidents` routes → `placeholder('Incidents')`), `ui/apps/web/src/routing/routeTable.ts` | `/incidents` and `/incidents/:id` are wired only to `PlaceholderPage`; no list, date-range filter, or detail component exists in `ui/apps/web/src/features`. |
| 244 | E6-S9-INFRA | NOT_STARTED | grep for `submit`, `SUBMISSION`, `neris-submit-queue`, `neris-status-queue`, `outbox` across `infrastructure/` and `backend/src/services/incident-service/` returns nothing beyond an unused `SUBMITTED` enum value | No status/retry routes, no `neris.submission.failed` rule/queue/DLQ, no status-consumer Lambda, no alarms. Depends on E6-S8 which also doesn't exist. |
| 169 | E6-S9-UI | NOT_STARTED | same as above; no incidents UI at all | No submission-status panel or retry control exists anywhere in `ui/apps/web`. |
| 243 | E6-S8-INFRA | NOT_STARTED | same grep as 244 | No `POST /submit` route, no outbox publisher off the incident-table Stream, no `neris-submit-queue`/DLQ, no NERIS Submission Worker Lambda, no prod-gate flag wiring. |
| 168 | E6-S8-UI | NOT_STARTED | same as above | No "Submit to NERIS" control anywhere in `ui/apps/web`. |
| 241 | E6-S6-INFRA | PARTIAL | `infrastructure/components/incident/incident.ts` (exposuresLambda + route), `backend/src/services/incident-service/putExposures.ts` | Route, table/CMK IAM, and Secondary-schema S3 read are wired (AC2 met). AC1 (narrower Cedar policy admitting only affected member/chief/safety officer on `INCIDENT_SECONDARY`) is explicitly NOT written — file comment: "safety officer" isn't one of the six Cedar role groups and the mapping decision was never made; route/IAM deployed with only the general VerifiedPermissions check. |
| 167 | E6-S6-UI | NOT_STARTED | no `features/incident*` dir in `ui/apps/web/src` | No exposure/responder-safety module UI exists. |
| 240 | E6-S5-INFRA | DONE | `infrastructure/components/incident/incident.ts` (responseTimesLambda + route), `backend/src/services/incident-service/putResponseTimes.ts`, `responseUnitRepository.ts` | Route + IAM (`PutItem`/`UpdateItem`/`Query`, no Scan) wired; `upsertResponseUnitTimes` only SETs fields present in the request, per-unit item keyed by `RESPONSE#{unitId}` — matches AC1–AC3. |
| 166 | E6-S5-UI | NOT_STARTED | no incidents feature dir in ui | No per-unit response-time editor exists. |
| 239 | E6-S4-INFRA | DONE | `infrastructure/components/incident/incident.ts` (narrativeLambda + route), `backend/src/services/incident-service/putNarrative.ts`, `repository.ts:64-69,264-276` | Route + narrow IAM (`GetItem`/`UpdateItem` + CMK only) wired; `MAX_NARRATIVE_LENGTH=25_000`, `NarrativeTooLongError` rejects oversize saves without truncating. |
| 165 | E6-S4-UI | NOT_STARTED | no incidents feature dir in ui | No narrative editor/char-count UI exists. |
| 238 | E6-S3-INFRA | PARTIAL | `infrastructure/components/incident/incident.ts` (updateLambda + route), `backend/src/services/incident-service/updateIncident.ts`, `schemaVersion/repository.ts` (`SCHEMA_VERSION_CACHE_TTL_MS`) | Route, IAM (GetItem/UpdateItem/Query + S3 schema-pin read + VP auth) and enum validation all work (AC1/AC3 met). AC2's Valkey-caching + VPC/security-group scope is not implemented — code comment in `incident.ts` states this Lambda deploys VPC-less because E8-S4-INFRA (Valkey/VPC/gateway endpoints) doesn't exist yet; schema lookups go straight to DynamoDB/S3 with an in-process cache instead. |
| 164 | E6-S3-UI | NOT_STARTED | no incidents feature dir or `@boxalarm/core` package in ui/ | No guided multi-step completion form exists; shared NERIS enum-validation package referenced by the ticket doesn't exist either. |
| 237 | E6-S2-INFRA | DONE | `infrastructure/components/incident/incident.ts` (createLambda, dispatch consumers), `backend/src/services/incident-service/createIncident.ts`, `dispatchAlertConsumer.ts`, `dispatchResponseConsumer.ts`, `infrastructure/components/messaging/queue-consumer.ts` | `POST /api/v1/incidents` route + IAM wired; consumers project `dispatch.alert.received`/`alerting.response.confirmed` off the platform bus into the incident table via `QueueConsumer` (built-in DLQ + `dlqDepthAlarm`, maxReceiveCount 5); create-lambda IAM has zero grant on the alerting table. Minor gap: `createLambda`'s IAM action list omits `dynamodb:TransactWriteItems` even though `repository.ts` issues a `TransactWriteCommand` on create — likely a runtime AccessDenied unless another statement covers it. |
| 163 | E6-S2-UI | NOT_STARTED | `ui/apps/web/src/App.tsx`, `ui/apps/web/src/lib/apiClient.ts` | Only a placeholder route exists; no "Create report from a dispatch" action or pre-filled detail view. |
| 93 | E6-S11 (parent) | DONE | `backend/src/services/incident-service/schemaVersion/{entity,repository,fixtures,validateEnum,refreshScanner/handler}.ts`, `validateEnum.test.ts` | `SCHEMA_VERSION` entity, ACTIVE pointer + TTL cache, refresh scanner publishing to S3+DDB without redeploy, and N/N-1 fixture-driven test matrix (`CORE_SCHEMA_V_N`, `CORE_SCHEMA_V_N_MINUS_1`) all present, satisfying AC1–AC4. |
| 92 | E6-S10 (parent) | DONE | `backend/src/services/incident-service/searchIncidents.ts`, `getIncident.ts` | Date-range search via GSI1 ordering by alarm time, single-request detail fetch including `corePayload` and `secondaryModules`. Row-level "no extra API call" (AC3) is a UI concern tracked by #170 (NOT_STARTED). |
| 91 | E6-S9 (parent) | NOT_STARTED | same grep as #244 | No submission-status route, retry route, or `neris.submission.failed` handling anywhere in the repo. |
| 90 | E6-S8 (parent) | NOT_STARTED | same grep as #244 | No outbox submit route, worker, or NERIS POST integration exists; only the OAuth2/client foundation (E6-S7) is built. |
| 89 | E6-S7 (parent) | DONE | `backend/src/services/incident-service/neris/{config,client,tokenCache}.ts` + `.test.ts` files | Per-env base URL/User-Agent/credentials from SSM+Secrets Manager (never hardcoded); `createNerisClient` structurally attaches `User-Agent`+`Authorization` on every request; `getAccessToken` refreshes at a 60s near-expiry skew with in-flight coalescing; `assertNonProductionBaseUrl` fails closed for non-prod stages resolving to `api.neris.fsri.org`. All 4 ACs covered with tests. |
| 88 | E6-S6 (parent) | DONE | `backend/src/services/incident-service/putExposures.ts`, `secondaryRepository.ts`, `getIncident.ts` | Creates `INCIDENT_SECONDARY` items with `secondaryType`/payload/`affectedMemberIds` (AC1); validates against Secondary schema enumerations before `complete` (AC2); each module returned as its own record via `queryIncidentSecondaries` (AC3). Narrower Cedar authorization is the INFRA-side gap (#241), not this backend piece. |
| 87 | E6-S5 (parent) | DONE | `backend/src/services/incident-service/putResponseTimes.ts`, `responseUnitRepository.ts` | Per-unit dispatched/enRoute/arrived/cleared independently settable and retrievable (AC1); partial-field SET preserves other timestamps/positions (AC2); each unit keyed by its own `RESPONSE#{unitId}` sk (AC3). |
| 86 | E6-S4 (parent) | DONE | `backend/src/services/incident-service/putNarrative.ts`, `repository.ts` | Narrative round-trips unchanged into `corePayload.narrative`; over-max-length save rejected with `NarrativeTooLongError` (no truncation). |
| 85 | E6-S3 (parent) | DONE | `backend/src/services/incident-service/updateIncident.ts`, `schemaVersion/validateEnum.ts` | Invalid enum values block the save and return field + allowed values (AC1); once `missingRequiredCoreFields` is empty, status flips to `VALIDATED` (AC2). AC3 (keyboard/focus/WCAG) is a UI concern — no UI exists (#164 NOT_STARTED). |
| 84 | E6-S2 (parent) | DONE | `backend/src/services/incident-service/createIncident.ts`, `dispatchProjection.ts` | `buildInputFromDispatch` pre-fills address/type/narrative/timestamps/units/members verbatim from the dispatch-copy + roster-copy projections (AC1/AC2); unknown `dispatchId` returns 404 via RFC 7807 `problemResponse` (AC3). |
